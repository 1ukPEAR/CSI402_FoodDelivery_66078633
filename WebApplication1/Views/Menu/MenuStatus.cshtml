@model List<WebApplication1.Models.db.FoodMenu>

@{
    Layout = "_NavbarEmployeeLayout";
    ViewData["Title"] = "สถานะเมนู";
}

<style>
    .menu-status-page {
        font-family: 'Prompt', sans-serif;
        color: #003049;
    }

    .simple-page-title {
        font-size: clamp(2rem, 3vw, 3rem);
        font-weight: 800;
        color: #003049;
        letter-spacing: -0.02em;
        line-height: 1.1;
    }

    .simple-page-subtitle {
        color: #5f6b76;
        font-size: 1rem;
        line-height: 1.6;
        font-weight: 400;
    }

    .menu-card {
        border: 1px solid #eadfca;
        border-radius: 22px;
        background: #fff;
        box-shadow: 0 10px 24px rgba(0, 0, 0, .035);
        overflow: hidden;
        transition: transform .2s ease, box-shadow .2s ease;
    }

    .menu-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 14px 28px rgba(0, 0, 0, .05);
    }

    .menu-img-wrap {
        padding: 14px 14px 0;
    }

    .menu-img {
        width: 100%;
        height: 210px;
        object-fit: cover;
        border-radius: 18px;
        display: block;
        background: #f8f6f0;
        border: 1px solid #f0e6d5;
    }

    .menu-name {
        font-size: 1.08rem;
        font-weight: 800;
        color: #003049;
        line-height: 1.35;
    }

    .menu-type {
        font-size: .84rem;
        color: #7b8794;
        font-weight: 600;
    }

    .menu-price {
        font-size: 1.35rem;
        font-weight: 800;
        color: #AA132A;
        line-height: 1.2;
    }

    .extra-box {
        background: #fffdf8;
        border: 1px solid #f0e6d5;
        border-radius: 16px;
        padding: 12px 14px;
    }

    .extra-box-empty {
        min-height: 92px;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .extra-title {
        font-size: .82rem;
        font-weight: 700;
        color: #6b7280;
        margin-bottom: 8px;
        text-transform: uppercase;
        letter-spacing: .02em;
    }

    .extra-list {
        list-style: none;
        padding-left: 0;
        margin: 0;
    }

    .extra-list li {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 10px;
        font-size: .9rem;
        color: #003049;
        padding: 4px 0;
        border-bottom: 1px dashed #eee4d2;
    }

    .extra-list li:last-child {
        border-bottom: none;
        padding-bottom: 0;
    }

    .extra-price {
        color: #AA132A;
        font-weight: 700;
        white-space: nowrap;
    }

    .extra-empty-text {
        font-size: .9rem;
        color: #9aa4af;
    }

    .status-box {
        border-top: 1px solid #f0e6d5;
        padding-top: 14px;
    }

    .status-label {
        font-size: .82rem;
        font-weight: 700;
        color: #6b7280;
        margin-bottom: 3px;
    }

    .status-text {
        font-size: .95rem;
        font-weight: 700;
    }

    .status-on {
        color: #198754;
    }

    .status-off {
        color: #6c757d;
    }

    .menu-status-switch {
        transform: scale(1.18);
        cursor: pointer;
        box-shadow: none !important;
    }

    .menu-status-switch:checked {
        background-color: #AA132A;
        border-color: #AA132A;
    }

    .menu-status-switch:focus {
        box-shadow: 0 0 0 .2rem rgba(170, 19, 42, .15) !important;
        border-color: #AA132A;
    }

    .empty-state {
        border: 1px solid #eadfca;
        border-radius: 22px;
        background: #fff;
        box-shadow: 0 8px 20px rgba(0, 0, 0, .03);
    }
</style>

<div class="container py-4 menu-status-page">

    <!-- SIMPLE PAGE HEADER -->
    <div class="mb-4">
        <h2 class="simple-page-title mb-2">สถานะเมนู</h2>
        <p class="simple-page-subtitle mb-0">
            พนักงานสามารถเปิด / ปิดสถานะเมนูได้อย่างเดียว
        </p>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show rounded-4 border-0 shadow-sm" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show rounded-4 border-0 shadow-sm" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- MENU GRID -->
    <div class="row g-4">
        @if (Model != null && Model.Any())
        {
            @foreach (var item in Model)
            {
                var imgPath = string.IsNullOrWhiteSpace(item.MenuImage)
                    ? "/img/menu/default-food.jpg"
                    : (item.MenuImage.StartsWith("/") ? item.MenuImage : "/img/menu/" + item.MenuImage);

                var rawMenuType = item.MenuType?.MenuTypeName?.Trim() ?? "-";
                var menuTypeName = rawMenuType.ToUpper() switch
                {
                    "FOOD" => "อาหาร",
                    "DRINK" => "เครื่องดื่ม",
                    "DESSERT" => "ของหวาน",
                    _ => rawMenuType
                };

                <div class="col-12 col-md-6 col-lg-4 col-xl-3">
                    <div class="card menu-card h-100">

                        <!-- IMAGE -->
                        <div class="menu-img-wrap">
                            <img src="@Url.Content(imgPath)"
                                 class="menu-img"
                                 alt="@(item.MenuName ?? "menu")"
                                 onerror="this.src='/img/menu/default-food.jpg'" />
                        </div>

                        <!-- BODY -->
                        <div class="card-body d-flex flex-column p-3 p-lg-4">

                            <div class="mb-2">
                                <h5 class="menu-name mb-1 text-truncate">
                                    @(item.MenuName ?? "ไม่ระบุชื่อเมนู")
                                </h5>

                                <div class="menu-type">
                                    ประเภทอาหาร: @menuTypeName
                                </div>
                            </div>

                            <div class="menu-price mb-3">
                                ฿@((item.MenuPrice ?? 0).ToString("0.00"))
                            </div>

                            <!-- EXTRA OPTIONS -->
                            @if (item.FoodMenuoptions != null && item.FoodMenuoptions.Any())
                            {
                                <div class="extra-box mb-3">
                                    <div class="extra-title">Extra Options</div>
                                    <ul class="extra-list mb-0">
                                        @foreach (var opt in item.FoodMenuoptions)
                                        {
                                            <li>
                                                <span>@opt.OptionName</span>
                                                <span class="extra-price">+฿@opt.ExtraPrice.ToString("0.00")</span>
                                            </li>
                                        }
                                    </ul>
                                </div>
                            }
                            else
                            {
                                <div class="extra-box extra-box-empty mb-3">
                                    <div class="extra-title">Extra Options</div>
                                    <div class="extra-empty-text">ไม่มีตัวเลือกเสริม</div>
                                </div>
                            }

                            <!-- STATUS SECTION -->
                            <div class="mt-auto status-box">
                                <div class="d-flex justify-content-between align-items-center gap-3">
                                    <div>
                                        <div class="status-label">สถานะเมนู</div>
                                        <div class="status-text @(item.MenuStatus == 1UL ? "status-on" : "status-off")">
                                            @(item.MenuStatus == 1UL ? "พร้อมขาย" : "ปิดเมนู / หมด")
                                        </div>
                                    </div>

                                    <form asp-controller="Menu" asp-action="ToggleMenuStatus" method="post" class="m-0">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@item.MenuId" />

                                        <div class="form-check form-switch m-0">
                                            <input class="form-check-input menu-status-switch"
                                                   type="checkbox"
                                                   @(item.MenuStatus == 1UL ? "checked" : "")
                                                   onchange="this.form.submit()">
                                        </div>
                                    </form>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <div class="empty-state text-center py-5">
                    <h5 class="mb-2 fw-bold">ยังไม่มีเมนูในระบบ</h5>
                    <p class="text-muted mb-0">
                        เมื่อมีเมนูเพิ่มเข้ามา จะสามารถจัดการสถานะได้จากหน้านี้
                    </p>
                </div>
            </div>
        }
    </div>
</div>